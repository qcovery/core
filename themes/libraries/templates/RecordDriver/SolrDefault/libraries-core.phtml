<?

$libraryList = $this->connectedlibraries()->getConnectedLibraries($this->searchClassId, $this->driver);

?>

<table class="table table-striped" summary="<?=$this->transEsc('Bibliographic Details')?>">
  <tr><th><?=$this->transEsc('Libraries')?>:</th><td>
    <p style="margin:0px">
      <? foreach ($libraryList as $library):?>
        <?=$library['abbrieviation']?> 
      <? endforeach;?>
    </p>
  </td></tr>
</table>
