<?php
$level_org = $level;
$label_org = $label;

echo $url;

$data = json_decode($data);

foreach($data->document[0]->item as $item) {
    foreach($item->service->available) {
        print_r($item);
    }
}

?>