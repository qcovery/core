<? foreach ($this->driver->getFormats() as $format): ?>
  <? if ($belugino = $this->configreader()->getConfigData($format)): ?>
    <i class="<?=$belugino?> belugino-icon-result-list" aria-hidden="true" title="<?=$this->transEsc($format) ?>"></i>
  <? else: ?>
    <i class="bel-unbek belugino-icon-result-list" aria-hidden="true" title="<?=$this->transEsc($format) ?>"></i>
  <? endif; ?>
<? endforeach; ?>
